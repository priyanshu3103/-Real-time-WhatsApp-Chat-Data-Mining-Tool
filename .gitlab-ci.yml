image: python:latest
stages:
  - build
  - test
  - deploy


build-app:
  stage: build
  script:
    - echo "Building app........"
    - pip install -r requirements.txt

test-app:
  stage: test
  script:
    - echo "Testing app......."
    - mkdir store

deploy-app:
  stage: deploy
  before_script:
    - streamlit --version
  script:
    - echo "Deploy app......."
    - streamlit run app.py
  artifacts:
    paths:
      - "store"
  only:
    - main